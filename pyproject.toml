[project]
name = "eps-brazil-cpl"
version = "0.0.0"
description = "Python utilities for the EPS Brazil model distribution."
requires-python = ">=3.14,<3.15"
dependencies = [
    "matplotlib",
    "openpyxl",
    "pandas",
    "windows-curses; sys_platform == 'win32'",
]

[tool.uv]
package = false